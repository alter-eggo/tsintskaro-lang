# Repository workflow

## Git

- Work directly on `main` by default.
- When asked to commit and push changes, commit them to `main` and push `origin/main`.
- Do not create feature branches or pull requests unless the user explicitly asks for them.
